id: sfro-rc-91
display_name: "Starfront Observatory - RedCat 91"
auth:
  token: something-super-secret
nina:
  host: "http://localhost:5000"
adapter:
  type: "sim"
# For real NINA testing, either set the legacy adapter above:
# adapter:
#   type: "nina"
#   host: "http://localhost:5000"
# or use the multi-adapter shape:
# adapters:
#   - id: "nina_main"
#     type: "nina"
#     host: "http://localhost:5000"